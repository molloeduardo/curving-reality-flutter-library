name: curving_reality_library
description: "A Flutter library that speeds up development with ready-to-use UI components, screens, and services to build high-quality iOS and Android apps from a single codebase"
version: 1.2.2
homepage: https://flutter-library.eduardomollo.com/
repository: https://github.com/molloeduardo/curving-reality-flutter-library
issue_tracker: https://github.com/molloeduardo/curving-reality-flutter-library/issues

environment:
  sdk: ">=3.2.6 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  animate_do: ^4.2.0
  crypto: ^3.0.6
  flutter:
    sdk: flutter
  font_awesome_flutter: ^10.7.0
  google_sign_in: ^6.3.0
  intl: ^0.20.2
  path: ^1.9.1
  shared_preferences: ^2.5.3
  sign_in_with_apple: ^7.0.1
  sqflite: ^2.4.2
  supabase_flutter: ^2.9.0
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  #assets:
